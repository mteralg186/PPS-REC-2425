<%- include('partes/cabecera') %>
<br>
<div class="container text-center mt-5">
    <h1>Algo fue mal</h1>
    <p>Ha ocurrido un error inesperado.</p>
                <% let homeUrl = '/'; %>
            <% if (typeof rol !== 'undefined') { %>
                <% if (rol === 'a') { homeUrl = '/alumno'; } %>
                <% if (rol === 'p') { homeUrl = '/profesor'; } %>
                <% if (rol === 'admin') { homeUrl = '/admin'; } %>
            <% } %>
    <a href="<%= homeUrl %>">Volver al inicio</a>
</div>
<%- include('partes/footer') %>